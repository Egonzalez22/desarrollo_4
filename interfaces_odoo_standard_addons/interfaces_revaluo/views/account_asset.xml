<odoo>
    <data>

        <!-- Sobreescribimos la vista de account.asset -->
        <record id="account_asset_view_form_inherit" model="ir.ui.view">
            <field name="name">account.asset.view.form.inherit</field>
            <field name="model">account.asset</field>
            <field name="inherit_id" ref="account_asset.view_account_asset_form" />
            <field name="arch" type="xml">

                <!-- valor residual porcentaje -->
                <xpath expr="//field[@name='salvage_value']" position="before">
                    <field name="valor_residual_porc" />
                </xpath>
            </field>
        </record>


    </data>
</odoo>